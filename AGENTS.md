# Prototype Instructions

This site is static HTML. Serve the folder and open `index.html` — do not start Vite or npm.

Keep `<nav data-approvedesk-auth></nav>` in the header so the page can drop into ApproveDesk `website/`.
